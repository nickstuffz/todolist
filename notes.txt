logic module:
    projects:
        -name
        -color-theme
        todo-items objects:
        - title
        - description
        - dueDate
        - priority

DOM module:
    - add / delete projects
    - add / delete todos
    - 


create using factory functions
(read documentation again)

UI design:
design in figma maybe?


add scrollbars for overflow tasks

Ctrl+C to kill web server

implement drag and drop to allow tasks to switch projects


STEP BACK

each project should be an object. Inside each project should be:

project.title = 'title'

project.tasks which is an array of task objects

method to display title

method to display tasks